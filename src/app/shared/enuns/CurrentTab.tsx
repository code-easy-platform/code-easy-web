export enum CurrentTab {
    editor = 'editor',
    plugins = 'plugins',
    properties = 'properties',
}

export enum CurrentResTreeTab {
    router = 'router',
    action = 'action',
    data = 'data',
}