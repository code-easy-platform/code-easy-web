/** Tipos de items existentes na toolbar. */
export enum ItemType {
    COMMENT = "COMMENT",
    FOREACH = "FOREACH",
    SWITCH = "SWITCH",
    ASSIGN = "ASSIGN",
    ACTION = "ACTION",
    START = "START",
    END = "END",
    IF = "IF",
}
